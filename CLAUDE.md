# Quick Refs

- Follow `AGENTS.md` for repo guidance.
- Package manager: Bun (`bun install --frozen-lockfile`).
- Main checks: `bun run typecheck`, `bun run test`, `bun run test:coverage`, `bun run build`.
